# AWS Bedrock Configuration for Alert Triage AI
# Copy this file to backend/.env and fill in your AWS credentials

# ===================================
# AWS BEDROCK CONFIGURATION (Required)
# ===================================
# Get your credentials from AWS IAM Console
# Guide: https://docs.aws.amazon.com/bedrock/latest/userguide/getting-started.html

# AWS Region (us-east-1 recommended for Amazon Nova Pro availability)
AWS_REGION=us-east-1

# AWS Credentials
# Option 1: Long-term IAM user credentials
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# Option 2: Temporary SSO/Federated credentials (if using AWS SSO)
# AWS_SESSION_TOKEN=your_session_token_here

# ===================================
# OPTIONAL DEMO SETTINGS
# ===================================
# Use cached responses for demo reliability (no API calls, no cost)
# Set to 'true' for instant demos
# Set to 'false' to make real API calls to AWS Bedrock
USE_CACHED_RESPONSES=false

# ===================================
# NOTES
# ===================================
# 1. NEVER commit the actual .env file with real credentials!
# 2. The .env file is in .gitignore to prevent accidental commits
# 3. For AWS Elastic Beanstalk deployment:
#    - Configure environment variables in EB Console
#    - Don't include credentials in deployment package
# 4. For production, use AWS IAM roles or AWS Secrets Manager
